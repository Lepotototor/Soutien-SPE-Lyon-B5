#include "array_even.h"

#include <malloc.h>

/*

  array_even function should create a sub-array forn the array given in
  parameters with it size
  you should create an dynamic array with all even values from arr
  the new array should have exactly needed size, no more, no less memory

  you should save the new array in the result variable and return the size

  Example: array_even({ 1, 2, 3, 4, 5, 6 }, 6, &result) = 3
            result = { 2, 3, 4 }

  if any error occur you should return 0

*/

size_t array_even(const int *arr, size_t len, int **result)
{
    (void)arr;
    (void)len;
    (void)result;
    return 0;
}
